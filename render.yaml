# Deploy lên Render.com: New > Blueprint > chọn repo này.
services:
  - type: web
    name: tho-ngoc-vs-zombie
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
